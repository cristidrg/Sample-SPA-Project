// import 'jquery';
// import 'kernl-ui';

window.Vue = require('vue');

// Vue.component('example-component', require('./components/ExampleComponent.vue'));

const app = new Vue({
    el: '#app',
    data: {
        title: 'Student Outcomes',
        tagline: 'We will stop the Robots'
    },
    methods: {
    }
});
